# Seclists

Seclists is a set of lists of known common words used for discovery & brute
force attacks. It can be obtained from git:

```bash
git clone https://github.com/danielmiessler/SecLists
```

... or through the apt repos, if available:

```bash
sudo apt install seclists
```
